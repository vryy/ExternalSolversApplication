message("")
message("**** configuring KratosExternalSolversApplication ****")

if(${CMAKE_SYSTEM_NAME} MATCHES "Android")
    message("KratosExternalSolversApplication is compiled on Android")
    include(${CMAKE_CURRENT_SOURCE_DIR}/CMakeLists.android.txt)
else()
    if((${PYTHON_VERSION_MAJOR} MATCHES "2") OR (${KRATOS_USE_BOOST_PYTHON} MATCHES ON))
        if((${PYTHON_VERSION_MAJOR} MATCHES "3") AND (${KRATOS_USE_BOOST_PYTHON} MATCHES ON))
            message("KratosExternalSolversApplication is compiled with Python 3 (Boost.Python)")
        else()
            message("KratosExternalSolversApplication is compiled with Python 2")
        endif()
        include(${CMAKE_CURRENT_SOURCE_DIR}/CMakeLists.unix.txt)
    elseif(${PYTHON_VERSION_MAJOR} MATCHES "3")
        message("KratosExternalSolversApplication is not compilable with Python 3")
    else()
        message("KratosExternalSolversApplication does not detect the Python version")
    endif()
endif()

message("**** finish configuring KratosExternalSolversApplication ****")
message("")
